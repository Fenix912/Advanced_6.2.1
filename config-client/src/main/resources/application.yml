server:
  port: 9999
spring:
  application:
    name: config-server
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
      spring:
        config:
          import: "optional:configserver:"
        cloud:
          config:
            enabled: true
            discovery:
              enabled: true